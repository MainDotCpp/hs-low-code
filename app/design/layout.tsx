import React from 'react';

export const metadata = {
  title: '落地页设计',
};
const DesignLayout = ({ children }: { children: React.JSX.Element }) => {
  return children;
};

export default DesignLayout;
